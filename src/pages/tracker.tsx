import Investment from "@/components/Investment";
import PageWrapper from "@/components/ui/PageWrapper";

const TrackerPage = () => {
  return <PageWrapper component={Investment} translationKey="tracker" />;
};

export default TrackerPage;
